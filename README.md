# Auto Battler V1

Vertical slice prototype: horizontal auto-combat, Ranger class, waves, XP, gold, Poison Arrow, admin balance panel, and JSON import/export.

Run with `python3 -m http.server 8000` and open `http://localhost:8000/`.
