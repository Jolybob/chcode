export class AutoBattlerHUD {
  constructor({engine,progression,data}){this.engine=engine;this.p=progression;this.data=data;this.host=document.getElementById('autoBattlerHud');this.returnFocus=null;if(!this.host)return;this.build();this.bind();this.update()}
  build(){this.host.innerHTML=`<div class="abh-top"><div class="abh-dungeon"><span class="abh-eyebrow">ADVENTURE</span><b data-dungeon>Dungeon</b><span data-wave>Wave 0 / 0</span></div><div class="abh-status"><span class="abh-auto"><i></i>AUTO</span><b data-state>COMBAT</b><small data-enemy>Scanning battlefield…</small></div></div><div class="abh-xp"><div><span>EXPERIENCE</span><b data-xp>0 / 100</b></div><i data-xp-fill></i></div><div class="abh-bottom"><div class="abh-portrait"><div class="abh-avatar" data-avatar>🏹</div><div><strong data-class>Hero</strong><small data-level>Level 1</small></div><div class="abh-bars"><div class="abh-hp"><span data-hp>0 / 1</span><i data-hp-fill></i></div><div class="abh-mana"><span data-mana>0 / 0</span><i data-mana-fill></i></div></div></div><div class="abh-hotbar-wrap"><div class="abh-hotbar-label"><span>ABILITIES</span><small>AUTO-CAST · MANA</small></div><div class="abh-hotbar" data-hotbar></div></div><div class="abh-currency"><button class="abh-shortcut" data-open-character title="Character sheet">☷</button><div><span>◈ <b data-gold>0</b></span><span>✦ <b data-points>0</b></span></div></div></div>`}
  bind(){this.host.querySelector('[data-open-character]')?.addEventListener('click',()=>document.getElementById('profileBtn')?.click())}
  update(){if(!this.host)return;const p=this.p,e=this.engine,c=this.data.classes?.[p.classId]||{},hero=e.hero;const hp=Math.max(0,Math.ceil(hero?.hp??0)),max=Math.max(1,Math.ceil(hero?.maxHp??1)),mana=Math.max(0,Math.floor(hero?.mana??0)),maxMana=Math.max(1,Math.ceil(hero?.maxMana??0));const xpNext=Math.max(1,p.xpToNext?.()||1),xpPct=Math.max(0,Math.min(100,p.xp/xpNext*100)),hpPct=Math.max(0,Math.min(100,hp/max*100)),manaPct=Math.max(0,Math.min(100,mana/maxMana*100));this.host.querySelector('[data-dungeon]').textContent=e.dungeon?.name||'Dungeon';this.host.querySelector('[data-wave]').textContent=`Wave ${e.wave||0} / ${e.dungeon?.waves||0}`;this.host.querySelector('[data-xp]').textContent=`${p.xp.toLocaleString()} / ${xpNext.toLocaleString()}`;this.host.querySelector('[data-xp-fill]').style.width=`${xpPct}%`;this.host.querySelector('[data-hp]').textContent=`${hp.toLocaleString()} / ${max.toLocaleString()}`;this.host.querySelector('[data-hp-fill]').style.width=`${hpPct}%`;this.host.querySelector('[data-mana]').textContent=`${mana.toLocaleString()} / ${maxMana.toLocaleString()}`;this.host.querySelector('[data-mana-fill]').style.width=`${manaPct}%`;this.host.querySelector('[data-class]').textContent=c.name||'Hero';this.host.querySelector('[data-level]').textContent=`Level ${p.level}`;this.host.querySelector('[data-gold]').textContent=p.gold.toLocaleString();this.host.querySelector('[data-points]').textContent=p.skillPoints;const enemy=e.enemies?.find?.(x=>!x?.dead);this.host.querySelector('[data-enemy]').textContent=enemy?.template?.name?`Target: ${enemy.template.name}`:'Waiting for target…';this.host.querySelector('[data-state]').textContent=e.state==='paused'?'PAUSED':e.state==='victory'?'VICTORY':e.state==='defeat'?'DEFEAT':'COMBAT';this.renderHotbar(c,hero)}
  renderHotbar(c,hero){const ids=c.spellIds||[],bar=this.host.querySelector('[data-hotbar]');if(bar.dataset.signature===`${c.id||''}:${ids.join(',')}`&&bar.dataset.tick===String(Math.floor((hero?.elapsed??performance.now())/200)))return;bar.dataset.signature=`${c.id||''}:${ids.join(',')}`;bar.innerHTML=Array.from({length:8},(_,i)=>{const id=ids[i],s=id?this.data.spells?.[id]:null;if(!s)return `<div class="abh-slot empty"><b>${i+1}</b></div>`;const cd=Math.max(0,hero?.spellTimers?.[id]??0),cost=Number(s.manaCost||0),ready=cd<=0&&Number(hero?.mana??0)>=cost;return `<div class="abh-slot filled ${ready?'ready':''}" title="${s.name||'Ability'} — ${s.type||'ability'} — ${cost} mana"><span>${s.icon||'✦'}</span><b>${i+1}</b><small>${cd>0?`${cd.toFixed(1)}s`:`${cost} MP`}</small></div>`}).join('');bar.dataset.tick=String(Math.floor(performance.now()/200))}
  render(){this.update()}
}
